name = "reportsthatmatter"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

rules = [
  { type = "Text", globs = ["**/*.md", "**/*.yaml"] }
]

[vars]
REPORTS_SOURCE = "bundled"

[dev]
local_protocol = "http"
